<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <conversionRule conversionWord="mask" class="com.shiro.warehouse.logging.PiiMaskingConverter"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %5p ${PID:- } --- [thread=%thread requestId=%X{requestId}] %-40.40logger{39}: %mask(%msg)&#10;%mask(%ex)</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    <appender name="ASYNC_CONSOLE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>8192</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>false</neverBlock>
        <includeCallerData>false</includeCallerData>
        <appender-ref ref="CONSOLE"/>
    </appender>
    <logger name="com.shiro.warehouse.logging.LoggingAspect" level="DEBUG"/>
    <root level="INFO"><appender-ref ref="ASYNC_CONSOLE"/></root>
</configuration>
